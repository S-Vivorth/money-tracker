<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".views.budget.BudgetFrag"
    android:background="?attr/secondaryBgColor">

    <!-- TODO: Update blank fragment layout -->
    <ImageView
        android:visibility="gone"
        android:id="@+id/logo_budget"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/budget_coming_soon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />
    <TextView
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/Coming_Soon"
        android:layout_marginTop="26dp"
        android:textSize="24sp"
        android:textColor="@color/light_natural7"
        android:fontFamily="@font/inter_600"
        android:textStyle="bold|normal"
        app:layout_constraintTop_toBottomOf="@+id/logo_budget"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/budgetRecyclerView"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center"
        android:id="@+id/noDataLayout"
        >

        <ImageView
            android:id="@+id/noDataIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/no_data_icon"
            app:tint="?attr/secondaryGreyTextColor"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/you_have_no_budget"
            style="@style/secondaryText"
            android:layout_marginTop="12dp"
            android:textColor="?attr/secondaryGreyTextColor"
            />

    </LinearLayout>

</RelativeLayout>