<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="68dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginStart="24dp"
    android:layout_marginBottom="12dp"
    android:layout_marginEnd="24dp"
    android:background="@drawable/rounded_subscription"
    android:orientation="horizontal"
    android:padding="12dp"

    >

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/price"
                style="@style/secondaryText"
                android:layout_width="wrap_content"
                android:layout_height="24dp"
                android:gravity="center_vertical"
                android:text="@string/yearly_price"
                android:textColor="@color/secondaryGreen"
                app:fontFamily="@font/inter_700"
                android:fontFamily="@font/inter_700"/>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="24dp"
                android:id="@+id/duration"
                android:textColor="@color/secondaryGreen"
                android:gravity="center_vertical"
                style="@style/secondaryText"
                app:fontFamily="@font/inter_500"
                android:fontFamily="@font/inter_500"
                />
        </LinearLayout>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="16dp"
            android:gravity="center_vertical"
            android:text="@string/with_seven_days_of_free_trial"
            android:textSize="12sp"
            android:id="@+id/description"
            android:textColor="?attr/secondaryGreyTextColor"
           />
    </LinearLayout>

    <androidx.cardview.widget.CardView
        app:cardBackgroundColor="@color/secondaryGreen"
        app:cardCornerRadius="38dp"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:id="@+id/saveLayout">

        <TextView
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/save_twenty_five_percentage"
            android:textColor="@color/white"
            android:textSize="12sp"/>
    </androidx.cardview.widget.CardView>
</RelativeLayout>
