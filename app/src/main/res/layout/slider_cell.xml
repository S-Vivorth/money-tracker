<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <LinearLayout
        android:layout_centerVertical="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/premium_slider_no_ads"
            android:id="@+id/sliderImg"
            />
        <TextView
            android:layout_marginTop="6dp"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:text="@string/remove_all_ads"
            style="@style/largeText"
            android:textColor="@color/default_og"
            android:textStyle="bold"
            android:id="@+id/sliderTitle"
            />
        <TextView
            android:maxLines="1"
            android:ellipsize="end"
            android:textAlignment="center"
            android:layout_marginTop="6dp"
            android:layout_width="match_parent"
            android:layout_height="24dp"
            android:text="@string/no_more_interruption"
            style="@style/secondaryText"
            android:textColor="@color/light_black_64"
            android:id="@+id/sliderDescription"
            />

    </LinearLayout>

</RelativeLayout>